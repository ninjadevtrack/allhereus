# Default values for allhere.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

instance: prod

replicaCount: 3

image:
  repository: registry.gitlab.com/allhere/platform_v1/web
  tag: v4a287c2
  pullPolicy: Always

nameOverride: ""
fullnameOverride: ""

service:
  type: NodePort
  port: 8000

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #  cpu: 100m
  #  memory: 128Mi
  # requests:
  #  cpu: 100m
  #  memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}

configMap:
  debug: "0"
  allowedHosts: ".eksprod.prod.allhere.co,0.0.0.0,127.0.0.1,.allhere.co"
  allowedCidrNets: 10.20.16.0/20
  dbHost: allhere-prod-db.c6dzfzqlepoa.us-east-2.rds.amazonaws.com
  dbName: allhere
  dbPort: 5432
  djangoStaticUrl: "https://a05-allherestatic-837582.s3.us-east-2.amazonaws.com/static/v4a287c2/static/"
  ednudgeApiUrl: "https://api.ednudge.com"

secret:
  dbUser: "myadmin"
  dbPassword: ""
  djangoSecretKey: ""
  ednudgeUsername: ""
  ednudgePassword: ""
